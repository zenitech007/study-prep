{
  "installCommand": "npm install --include=optional",
  "buildCommand": "npm run build --workspace=nsg215-app",
  "outputDirectory": "nsg215-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
